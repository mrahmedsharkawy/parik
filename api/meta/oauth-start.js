import crypto from "node:crypto";import {setCookie} from "./_lib.js";
export default async function handler(req,res){const appId=process.env.META_APP_ID,redirect=process.env.META_REDIRECT_URI;if(!appId||!redirect)return res.status(500).send("Meta OAuth env is not configured");const state=crypto.randomBytes(24).toString("hex");setCookie(res,"bariq_meta_oauth_state",state,600);const u=new URL("https://www.facebook.com/v26.0/dialog/oauth");u.searchParams.set("client_id",appId);u.searchParams.set("redirect_uri",redirect);u.searchParams.set("state",state);u.searchParams.set("scope","ads_management,ads_read,business_management,pages_show_list,pages_read_engagement,instagram_basic");res.redirect(302,u.toString())}